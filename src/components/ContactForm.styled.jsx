import styled from 'styled-components';

const StyledForm = styled.form`
  display: flex;
  flex-direction: column;
`;

const StyledLabel = styled.label`
  margin-bottom: 20px;
`;

const SubmitBbutton = styled.button`
  width: 140px;
`;

export { StyledForm, SubmitBbutton, StyledLabel };
